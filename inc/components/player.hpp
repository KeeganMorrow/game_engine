#ifndef COMPONENTS_PLAYER_HPP
#define COMPONENTS_PLAYER_HPP
namespace components{
    struct Player{
        Player():x(0){}
        // Empty struct for now, just indicates that this is the player.
        int x;
    };
}
#endif /* COMPONENTS_PLAYER_HPP */
